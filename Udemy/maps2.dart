mains() {
  //Para definir o tipo é como no array, porém define-se primeiro o tipo da chave e depois o tipo do item agregado.
  Map<String, dynamic> maps = {
    "nome": "Josué",
    "idade": 44,
  };
  print(maps);
}
