main() {
  List<String> textos = [
    "qualquer coisa",
    "mais alguma coisa"
  ]; // Para definir o tipo de dado que a lista suporta podemos inseri-lo entre os sinais < >
  textos
      .add(4); //observa-se que o valor gera um erro por não ser do tipo string.
}
